<view class="post-container" wx:for="{{postList}}" wx:key="index">
  <view class="header">
    <view class="left">
      <image src="{{item.avatar}}"></image>
      <text>{{item.userName}}</text>
    </view>
    <image class="more" src="../../static/more.svg" bindtap="reportPost" data-id="{{item.postId}}"></image>
  </view>
  <view class="second-line">
    <view class="title">{{item.title}}</view>
    <view class="right">
      <view class="contact" bindtap="showContactInfo" data-info="{{item.contactInfo}}">
        <image src="../../static/contact.svg"></image>
        <text>联系</text>
      </view>
      <view class="price">{{item.price}}</view>
    </view>
  </view>
  <view class="content" bindtap="enterPost" data-id="{{item.postId}}">
    {{item.content}}</view>
  <view class="location">
    <text>{{item.location}} </text><text> {{item.createAt}}</text>
  </view>
  <view class="footer">
    <view class="item">
      <image src="../../static/share.svg"></image>
      <text>分享</text>
    </view>
    <view class="right">
      <view class="follow item" data-id="{{item.postId}}" data-index="{{index}}" bindtap="toggleFollow">
        <image src="{{item.isFollow?'../../static/followed.svg':'../../static/follow.svg'}}">
        </image><text>{{item.follow}}</text>
      </view>
      <view class="reply item">
        <image src="{{item.isReplied?'../../static/replied.svg':'../../static/reply.svg'}}"></image><text>{{item.reply}}</text>
      </view>
      <view class="thumb item" data-id="{{item.postId}}" data-index="{{index}}" bindtap="toggleThumbup">
        <image src="{{item.isThumb?'../../static/thumbed.svg':'../../static/thumb.svg'}}"></image><text>{{item.thumb}}</text>
      </view>
    </view>
  </view>
</view>
<view class="contactInfoContainer overlay" wx:if="{{contactInfoShow}}" bindtap="hideContactInfo">
  <view class="contactInfo">
    <view class="content" catchtap="copyContactInfo" data-info="{{contactInfo.qq}}" data-type="qq"><text>Q Q： {{contactInfo.qq}}</text>
      <text class="copy">copy</text>
    </view>
    <view class="divider"></view>
    <view class="content" catchtap="copyContactInfo" data-info="{{contactInfo.wx}}" data-type="wx">
      <view>微信： {{contactInfo.wx}}</view>
      <text class="copy">copy</text>
    </view>
  </view>
</view>
<!-- <view class="reportContainer overlay"></view> -->